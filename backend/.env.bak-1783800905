# MongoDB
MONGODB_URI=mongodb+srv://mohamedzaherdev_db_user:KdK9Vz9V4eRz1smV@newenergizelogistics.jwygwrm.mongodb.net/?appName=newEnergizeLogistics

# JWT
JWT_ACCESS_SECRET=enrg-acc-s3cret-k3y-pr0duct10n-r3ady-2024!
JWT_REFRESH_SECRET=enrg-ref-s3cret-k3y-pr0duct10n-r3ady-2024!
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=7d

# Server
PORT=5001
NODE_ENV=development

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# Seed Admin
SEED_ADMIN_EMAIL=admin@energize.com
SEED_ADMIN_PASSWORD=ChangeThisPassword123!
SEED_ADMIN_FIRST_NAME=Super
SEED_ADMIN_LAST_NAME=Admin

# UPL / Operations external system (B2B: Fleet Management + 3PL)
UPL_BASE_URL=https://backend.energize-logistics.com
UPL_API_KEY=061e9b4d50de7a1b291f7f6c90165d1e3105a48a1d33d5a63db443a229a03db1
UPL_ADMIN_EMAIL=admin@admin.com
UPL_ADMIN_PASSWORD=056936501611
# Near-real-time polling: shipments (fast) + stats (slower). Lower = snappier, more load on UPL.
UPL_POLL_INTERVAL_MS=6000
UPL_STATS_INTERVAL_MS=30000
# Shared secret for the inbound UPL webhook (POST /api/ops/webhook) — set the same value on UPL's side for genuinely-instant push.
UPL_WEBHOOK_SECRET=enrg-ops-webhook-s3cret-change-me
# How often (minutes) to mirror UPL customers into the CRM
OPS_CUSTOMER_SYNC_MIN=30

# Location Solutions (Wialon) GPS/telemetry — قسم لوكيشن سوليوشن
# Endpoint is /wialon/ajax.html (NOT /lsx — that 404s). Auth is token → session.
LS2_BASE_URL=https://ls2a.locationsolutions.com/wialon/ajax.html
LS2_TOKEN=eefa254a746f1db2eafde0524833c6f8E84E1B5C4F0863B04CBB51AF91E3B0F1957A1801
# Live poll cadence (ms) — decode sensors, evaluate alerts, mirror snapshot.
LS2_POLL_INTERVAL_MS=15000
# Safety-net full reconciliation for UPL shipments → Operations workflow.
# NOT the live mechanism — live updates arrive within ~6s via the poll. This just
# re-scans everything periodically to catch anything the live poll might miss.
UPL_WORKFLOW_SYNC_MIN=60
