'use client';
// Kept as a route for existing links and bookmarks — the store page is where
// this now lives in the sidebar.
import { InventoryPanel } from '@/components/workshop/InventoryPanel';

export default function Page() {
  return <InventoryPanel />;
}
