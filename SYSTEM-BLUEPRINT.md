# Energize Logistics — خريطة النظام الكاملة

## System Blueprint — sections, pages, workflows & mechanics

> الملف ده مكتوب عشان يتاخد **كوبي/بيست** لمشروع تاني: كل قسم فيه صفحاته، وبيحصل
> فيه إيه، والورك فلو بتاعه من أول خطوة لآخر خطوة، وإزاي الأقسام مربوطة ببعض.
>
> This file is written to be copy-pasted into another project as a spec: every
> section, its pages, what happens in each, the full workflow, and how sections
> connect to each other.
>
> الأرقام والأسماء هنا كلها مقروءة من الكود الشغّال فعلًا، مش تقديرية.
> Everything here is read from the live codebase, not estimated.

---

# ٠. الأرقام في سطر واحد — At a glance

|                                                |                                                                                    |
| ---------------------------------------------- | ---------------------------------------------------------------------------------- |
| الأقسام المُدارة بالصلاحيات · Managed sections | **٢٢**                                                                             |
| صفحات `/system` · Pages                        | **٢٥٤**                                                                            |
| مدخل في القايمة الجانبية · Nav entries         | **٢٢٤**                                                                            |
| الأدوار · Roles                                | **٤٩** (٥ عامة + ٢١ مدير قسم + ٢٣ موظف قسم — إدارة العلاقات وحدها عندها ٤ مستويات) |
| مسارات الـAPI · API route groups               | **٥١**                                                                             |
| الواجهة · Frontend                             | Next.js (App Router) + TypeScript + Tailwind                                       |
| الخلفية · Backend                              | Express + MongoDB (Mongoose) + Socket.IO                                           |
| الموبايل · Mobile                              | Flutter (نفس الـAPI بـ Bearer token)                                               |
| اللغات · Languages                             | عربي/إنجليزي كامل + RTL                                                            |

---

# ١. الأساسات — الحاجات اللي كل قسم بيقف عليها

## Foundations — the mechanics every section reuses

دي أهم جزء لو هتنقل الفكرة لمشروع تاني. الـ٢٢ قسم **مش ٢٢ تطبيق**؛ دول ٢٢ استعمال
لنفس ٨ آليات.

### ١.١ نظام الصلاحيات الديناميكي · Dynamic permissions

```
config/sections.js   →  قايمة الأقسام: key + apiPrefixes + defaultRoles
config/roles.js      →  ٤٩ دور، لكل قسم مدير + موظف، اسم عربي وإنجليزي
middleware/sectionGate.js →  بيتركّب على apiPrefixes بتاع كل قسم
```

**الفكرة:** super_admin عنده صفحة «الأدوار والصلاحيات» بيدي فيها كل **دور × قسم**
واحد من تلاتة: `none` / `view` / `edit`.

- **الواجهة:** القسم بيظهر في القايمة الجانبية بس لو صلاحية الدور `view` أو `edit`.
- **الخلفية:** `sectionGate` بيقفل الكتابة على `view` ويقفل كل حاجة على `none`،
  وبيحطّ `req.sectionAccess` فـ `authorize()` تقدر تسمح لدور اتمنح الصلاحية حديثًا.
- **ضمان عدم القفل (no-lockout):** الدور اللي مالوش سجل صلاحيات محفوظ بيعدّي على
  القوايم القديمة زي ما هو — يعني تشغيل النظام لأول مرة ما بيكسرش حاجة.
- **الفولباك بيوسّع بس، عمره ما يضيّق.** لو مفيش قاعدة صريحة، الدور بياخد صلاحية
  قسمه الافتراضية؛ ولو دي `none` بيكمل للقوايم القديمة بدل ما يترفض.
- **قاعدة مفروضة وقت التحميل:** كل قسم لازم يكون له مدير وموظف، والدور اللي اسمه
  بينتهي بـ`_manager` بيتعامل كمدير في كل النظام (مراجعة الأعمال بتقرا اللاحقة دي
  حرفيًا). `assertRolesCoverSections()` بترمي خطأ وقت الإقلاع لو قسم ناقصه دور.

**التصميم اللي يتنقل:** خلي القسم **مفتوح بالكامل بكل أكشناته** لأي حد اتفتح له،
والفرق بين المدير والموظف يكون في _نطاق الرؤية_ (بيشوف مين) مش في _الأزرار_.

### ١.٢ المهام والشكاوى لكل قسم · Per-section Tasks & Complaints

كل قسم عنده صفحتين متكرّرتين: **«مهامي»** و**«الشكاوى»** — من نفس المكوّن ونفس
الـAPI (`/api/section-work`).

**قاعدة الرؤية صارمة:** السجل المُسند لشخص يشوفه (أ) المُسنَد إليه، (ب) اللي أنشأه،
(ج) super_admin — **وبس**. حتى مدير نفس القسم ما بيشوفهوش.

أول ما تتسند مهمة، المُسنَد إليه بياخد إشعار لحظي على الويب وإشعار بصوت على
الموبايل.

### ١.٣ الإشعارات · Notifications

`createNotification({ recipient, type, title, message, relatedEntity, relatedEntityId })`

بتعمل حاجتين مع بعض: بتحفظ في قاعدة البيانات (عشان الجرس يفضل شايفها بعد ريفريش)،
وبتبعت `notification:new` على السوكت للمستخدم ده بالذات (عشان تظهر من غير ريفريش).
الموبايل بيحوّلها لإشعار نظام + رقم على الأيقونة.

### ١.٤ التحديث الحي · Live updates

كل قسم بيبثّ حدث سوكت باسمه (`hr:master`, `ls2:updated`, `fleet:updated` …).
الصفحات بتشترك فيه بـ`useSocket(event, reload)`. النتيجة: أي تغيير من أي مستخدم
على أي جهاز بيوصل لكل الشاشات المفتوحة من غير ريفريش.

مع كاش TTL قصير (١٢–٢٠ ثانية) على الاستعلامات التقيلة، **بيتمسح فورًا** عند أي
كتابة عشان ما يفضلش يعرض القديم.

### ١.٥ القوائم المرجعية · Lookups / reference data

بدل ما كل قايمة منسدلة تبقى ثابتة في الكود، فيه نظام واحد (`/api/lookups`) وصفحة
«القوائم المرجعية» في الإدارة. ومكوّن `ManagedSelect` بيسمح بإضافة قيمة جديدة
**من نفس مكان الاستعمال** من غير ما المستخدم يسيب الشاشة.

### ١.٦ محرك التقارير · Reporting engine

`/api/reports` بياخد «مستند بلوكات» (blocks) ويطلّع PDF على ورق الشركة الرسمي لأي
كيان: مركبة / سائق / عميل / مورد / موظف / قسم.

**درس مهم اتدفع تمنه:** في الطباعة `position: fixed` بيتحسب على صندوق محتوى الصفحة
مش على الصفحة — يعني الورق الرسمي بيتزحلق. الحل: توليد المحتوى بـ`omitBackground`
وبعدين تركيب الورق الرسمي بـ`pdf-lib` كطبقة تحت — والنص بيفضل قابل للتحديد.

### ١.٧ الملفات الرئيسية (Masters) والاستيراد · Master files & imports

الأقسام اللي داتاها جاية من شيتات (الموارد البشرية، المركبات، الكاوتش) عندها ملف
JSON مرجعي واحد وسكربت استيراد **idempotent**: تشغّله عشر مرات يطلّع نفس النتيجة.

قواعد اتعلمناها بالغلط:

- **اربط بالمعرّف الثابت، مش بالرقم المتسلسل.** الرقم الوظيفي بيتعاد استخدامه لموظف
  تاني، ورقم اللوحة بيتغيّر والشاسيه لأ. اربط برقم الهوية / الشاسيه / السيريال.
- **«مطلوب» تصريح مش سكوت.** لو الشيت قال «الحقل ده ناقص»، امسح القيمة القديمة
  البايتة. الفرق ده وحده صحّح ١٦٥٢ حقل ومنع «متأخر ٧٨٦٢ يوم».
- **ارفض التناقض، ما تبلعوش.** نفس السيريال على عربيتين = وقف واطبع، مش آخر صف
  يكسب.
- **خُد نسخة احتياطية قبل أي مسح**، واحتفظ بالـ`_id` القديم لما تعيد البناء عشان
  مراجع الأقسام التانية ما تتكسرش.

### ١.٨ التدقيق المستمر · Permanent audit scripts

لكل قسم كبير سكربت تدقيق بيشتغل على **الـAPI الحقيقي والداتا الحقيقية** (مش وحدات
اختبار وهمية) وبيطبع «نجح كذا · فشل كذا».

قواعد التدقيق:

- مستخدمو الاختبار بيبدأوا بـ`zz-` وبيتمسحوا في الآخر.
- التيست اللي بيكتب لازم يكتب على **داتا بتاعته** (عربية اختبار بيعملها ويمسحها)،
  عمره ما يلمس سجل شغّال.
- في الآخر تشيك: «التدقيق ما سابش أي أثر».

المجموع الحالي: **~٢٩٣ تشيك** موزّعين على ١٣ سكربت.

---

# ٢. الأقسام — Sections

## كل قسم: صفحاته، بيحصل فيه إيه، والورك فلو

---

## ٢.١ الموارد البشرية · HR

`/api/hr` — الأدوار: `hr_manager` / `hr_specialist`

### الصفحات

**ملف الموظفين (الماستر)** — كل عمود في الشيت له كارت بعدّاده:
| الصفحة | بيحصل فيها إيه |
|---|---|
| `/system/hr/master` | النظرة الشاملة: كارت لكل حقل بعدّاد «مطلوب / غير مطلوب / مملي»، الضغط على أي رقم بيفتح الناس اللي وراه |
| `/system/hr/master/expiring` | كل المستندات اللي بتنتهي، بفلتر مدة حر (٣٠/٦٠/٩٠/أي رقم) |
| `/system/hr/master/iqama` | الإقامات |
| `/system/hr/master/passport` | الجوازات |
| `/system/hr/master/contract` | بيانات العقود (حقول الشيت) |
| `/system/hr/master/medicalInsurance` | التأمين الطبي |
| `/system/hr/master/healthCertificate` | الشهادات الصحية |
| `/system/hr/master/driverCard` | بطاقات السائقين |
| `/system/hr/master/drivingLicense` | رخص القيادة |
| `/system/hr/master/gosi` | التأمينات الاجتماعية |
| `/system/hr/master/banking` | البيانات البنكية والآيبان |

**العمليات اليومية:**
| الصفحة | بيحصل فيها إيه |
|---|---|
| `/system/hr/employees` | سجل الموظفين + ملف كل موظف (تعديل، مستندات، تجديدات، إنهاء خدمة، سجل التغييرات) |
| `/system/hr/contracts` | عقود التوظيف: إنشاء، تجديد، إنهاء |
| `/system/hr/leaves` | كل طلبات الإجازة وقرارها |
| `/system/hr/requests` | طلبات الموظفين (خطابات، شهادات، استفسارات) |
| `/system/hr/custody` | عهد الموظفين |
| `/system/hr/stock` | مستودع الموارد البشرية (اللي بيتصرف منه العهد) |
| `/system/hr/licenses` | تراخيص واشتراكات الشركة وتواريخ تجديدها |
| `/system/hr/leave-types` | أنواع الإجازات وسياسة كل نوع |
| `/system/hr/my-tasks` · `/complaints` · `/kpis` | المتكرّرة في كل قسم |

**الخدمة الذاتية (لكل الموظفين):**
`/system/hr/me` (ملفي) · `/system/hr/my-leaves` (إجازاتي) · `/system/hr/my-requests` (طلباتي)

### الورك فلو ①: طلب إجازة — من أول ما الموظف يفتح الشاشة لحد التوقيع

```
١) الموظف يفتح «إجازاتي» → طلب جديد
   • يختار النوع + من تاريخ + لتاريخ
   • النظام يحسب عدد الأيام ويعرض **رصيده الحالي** والمتبقّي بعد الطلب

٢) فحص الإشعار المسبق (advance notice)
   • كل نوع إجازة له سياسة: الإجازة المخططة محتاجة ٣٠ يوم إشعار
   • الطارئة والمرضية معفيّة
   • لو الشرط مش متحقق → الطلب يترفض برسالة فيها الرقمين:
     «محتاج ٣٠ يوم وإنت طالب قبلها بـ٥»
   • الموارد البشرية وحدها تقدر تعمل تجاوز صريح (policyOverride)

٣) الحفظ: النظام بياخد **لقطة للرصيد** (accrued / available / remainingAfter)
   ويحطها جوه الطلب — عشان بعد شهور تعرف الرصيد كان كام وقت الموافقة

٤) التوجيه التلقائي
   • للموظف مدير؟  → status = pending_manager, currentStage = manager
   • مالوش مدير؟   → status = pending_hr,      currentStage = hr
   • المدير بياخد إشعار لحظي

٥) قرار المدير  (PATCH /leaves/:id/decision)
   • رفض → انتهى الطلب فورًا
   • موافقة → يتحوّل للموارد البشرية (pending_hr)
   • المدير يقدر **يوقّع** — التوقيع بيتاخد منه لقطة على القرار ده

٦) قرار الموارد البشرية = القرار النهائي
   • موافقة → approved، الرصيد يتخصم، إشعار للموظف
   • رفض → rejected

٧) الطباعة: الطلب بيتطبع PDF على ورق الشركة وفيه التوقيعات

الإلغاء: الموظف يقدر يلغي طلبه ما دام لسه مش نهائي
```

### الورك فلو ②: جمع البيانات الناقصة (الحاجة اللي بتخلّي الشيت شغل)

```
١) الملف الرئيسي بيقول لكل حقل حالته:
     required     = ناقص، لازم يتجمّع     ← ده شغل
     not_required = لا ينطبق على الشخص ده  ← ده مش شغل
     filled       = موجود
   الفرق بين الاتنين الأولانيين هو اللي بيحوّل الداشبورد من أرقام لقايمة شغل

٢) الشريط العلوي بيقول عدد الناقص في كل صفحة بالأحمر
   → ترتيب شغل اليوم بقى قراءة سطر واحد

٣) تدوس على العدد → تفتح الناس اللي وراه بالظبط

٤) تملّي الخانة **من نفس الجدول** (تدوس عليها وتكتب)

٥) الحفظ بيشيل حالة «مطلوب» تلقائيًا → العدّاد فوق بينقص لوحده
   (من غير ده، الفريق بيشتغل والرقم مكانه فيفضلوا يدوّروا على شغل خلص)
```

### قواعد اتفرضت في الموارد البشرية (تنفع لأي مشروع)

- **اليوزر مش موظف.** إنشاء حساب دخول بيعمل ملف خدمة ذاتية، **مش** سجل موظف. من
  غير القاعدة دي، كل حساب جديد بيزوّد «عدد الموظفين» واحد.
- **رقم الهوية في كل جدول.** ده اللي بيتسيرش بيه فعليًا، فليه عمود في كل شاشة.
- **الكارت ما يكدبش.** «عدد الموظفين» بيتحسب من الملف كله، ما بيتحركش مع الفلاتر؛
  اللي بيتحرك بيتكتب جنبه «الأرقام محسوبة على N».

---

## ٢.٢ لوكيشن سوليوشن (النقل الثقيل) · Location Solutions

`/api/ls2` — الأدوار: `location_manager` / `location_staff`

### الصفحات

| الصفحة                                                    | بيحصل فيها إيه                                    |
| --------------------------------------------------------- | ------------------------------------------------- |
| `/system/ls2`                                             | لوحة التتبّع                                      |
| `/system/ls2/live`                                        | الأسطول المباشر على الخريطة (تكامل GPS)           |
| `/system/ls2/registry`                                    | سجل الأسطول                                       |
| `/system/ls2/drivers` · `/driver-performance`             | السواقين وتقييمهم من بيانات التليمتري             |
| `/system/ls2/tires`                                       | ضغط وحرارة الكاوتش الحيّة من الحساسات             |
| `/system/ls2/temperature`                                 | حرارة المبرّد                                     |
| `/system/ls2/maintenance`                                 | الصيانة الدورية بالكيلومترات                      |
| `/system/ls2/fleet-assets`                                | **السطحات والتيدرات وفردات الكاوتش** (سجل الورشة) |
| `/system/ls2/store`                                       | مخزن النقل الثقيل                                 |
| `/system/ls2/repairs`                                     | الصيانة الاستثنائية                               |
| `/system/ls2/alerts` · `/reports` · `/settings` · `/kpis` |                                                   |

### الفكرة المعمارية المهمة

فيه **مصدرين مختلفين تمامًا** لنفس العربية، وخلطهم غلط:

- **البثّ الحيّ (GPS/telemetry):** بيعرف الحساسات — ضغط، حرارة، سرعة، عدّاد.
  ما بيعرفش **أنهي فردة** (بالسيريال) قاعدة على الموقع ٩.
- **سجل الأصول (الورشة):** بيعرف كل فردة بسيريالها وتاريخها. ما بيعرفش الضغط.

الربط بينهم بـ**مفتاح اللوحة المطبّع** (أرقام بس)، لأن نصوص اللوحات مختلفة بين
النظامين. «5010» و«أ ص ر 5010» لازم يبقوا نفس العربية.

### الورك فلو ③: تركيب / إنزال فردة كاوتش

```
تقسيمة العربية الكاملة = ١٤ فردة:  ٦ رأس + ٦ تيدر + ٢ استبن

دورة حياة الفردة:
  spare(new) ──تركيب──► mounted ──إنزال──► spare / in_repair / scrap / damaged
                                    │
                                    └─ in_repair ──نتيجة──► spare(renewed) أو scrap
                                                             │
                                                    المجدد يتركّب على **التيدر بس**
                                                    (الرأس هو أخطر مكان للانفجار)

قاعدة الداخل ⇐ الخارج:
  لو ركّبت فردة في موقع **مشغول**، لازم تقول الفردة القديمة راحت فين:
  تحت التجديد / تالفة / سكراب / سليمة للمخزن (وبنسبة حالتها ٪)

قاعدة «الموقع ما بيفضلش فاضي»:
  الفردة ما تنزلش من غير ما حاجة تتركب مكانها أو تتبدّل بيها.
  مفروضة على **تلات أبواب** مش واحد:
     POST /move        (الإنزال)
     POST /retire      (تالفة / سكراب)
     POST /status      (نقل الحالة)
  لو اتحطت في الإنزال بس، الشغل بيتسرّب من «تالفة» ويسيب الموقع فاضي برضه.
  الاستثناء الوحيد: **الاستبن** — العربية بتمشي من غيره فعلًا.

كل حركة بتتكتب في سجل append-only (registered / mounted / transferred /
removed / to_repair / renewed / scrapped / damaged / sold) — مفيش سطر بيتعدّل.
```

### الورك فلو ④: «العربية دي عملت فيها إيه؟»

سؤال إجابته كانت متفرّقة على خمس شاشات، فاتدمجت في خط زمني واحد:

```
GET /assets/vehicle/:plate/history
   ├── حركة الكاوتش      ← سجل الأصول
   ├── قطع الغيار        ← مخزن النقل الثقيل + مخزن الورشة
   ├── الإصلاحات         ← الصيانة الاستثنائية
   └── الصيانة الدورية   ← سجل الخدمة
   كلهم مرتبين زمنيًا، كل نوع بلونه وبفلتر
```

وعدد الكاوتش في الجدول **زرار**: أخضر لو ١٤/١٤، أصفر لو ناقص، رمادي لو فاضي —
والضغط بيفتح الفردات ببياناتها وبنفس الأكشنز اللي في الجدول الرئيسي.

**درس معماري:** صفّ الأزرار اتحطّ في **مكوّن واحد** بيستعمله المكانين. لو اتنسخ،
أول تعديل في قواعد الظهور هيخلّي الشاشتين مختلفتين.

---

## ٢.٣ إدارة الأسطول · Fleet Management

`/api/fleet` — الأدوار: `fleet_manager` / `fleet_supervisor`

عربياتنا إحنا (مقابل «طلبات الشحنات» اللي بتحجز عربيات موردين).

| الصفحة                                   | بيحصل فيها إيه            |
| ---------------------------------------- | ------------------------- |
| `/system/fleet/board`                    | لوحة تشغيل بأعمدة الحالات |
| `/system/fleet`                          | الحمولات                  |
| `/system/fleet/dashboard`                | التحليلات                 |
| `/system/fleet/drivers` · `/driver-kpis` | السواقين وتقييمهم         |
| `/system/fleet/vehicles`                 | سياراتنا                  |
| `/system/fleet/assign`                   | توزيع المشرفين            |
| `/system/fleet/customers` · `/settings`  |                           |

### الورك فلو ⑤: حمولة من الطلب للفاتورة

```
requesting → loading → uploaded → on_way → arrived
                                              ├→ bond_sent → bond_received → invoiced
                                              └→ late  (لو فات الوقت المتوقع)
                                            cancelled  (في أي وقت)

القواعد:
  • السائق يقعد على العربية، وأقصى ٢ سواقين للعربية الواحدة
  • متابعة كل ٣ ساعات — آخر تواصل بيتسجّل على الحمولة
  • كل تغيير حالة / تغيير سائق / متابعة بيتسجّل حدث في السجل
  • أرقام البوليصة بتبدأ من 100001
  • البوليصة PDF بتتولّد **على السيرفر** عشان الموبايل والويب يطلّعوا نفس الورقة
  • تصدير مجمّع لكل البوالص في ملف ZIP
```

---

## ٢.٤ التخليص الجمركي · Customs

`/api/customs-clearance` — الأدوار: `customs_manager` / `customs_officer`

`/system/customs` (خط الأنابيب) · `/system/customs/guide` (دليل الإجراءات)

### الورك فلو ⑥: معاملة تخليص — ١١ مرحلة

```
 ١) papers_received         استلام الأوراق
 ٢) declaration_paid        طباعة البيان الجمركي وسداده
 ٣) do_requested            إرسال أوراق الوكيل وطلب فاتورة إذن التسليم
 ٤) do_linked               ربط إذن التسليم
 ٥) port_fees_paid          طباعة فاتورة أجور الموانئ وسدادها
 ٦) unloading_fees_paid     طباعة فاتورة أجور التفريغ وسدادها
 ٧) transport_order         عمل أمر نقل
 ٨) containers_transported  نقل الحاويات للعميل أو الساحة
 ٩) unloaded_stored         التفريغ والتخزين
١٠) containers_returned     إرجاع الحاويات للوكيل
١١) invoiced               عمل الفواتير

لكل مرحلة: تاريخ + علامة إتمام + شيك ليست
وقوالب إيميلات جاهزة للوكلاء
```

---

## ٢.٥ المركبات والتفاويض · Vehicles

`/api/vehicles` — الأدوار: `vehicles_manager` / `vehicles_staff`

| الصفحة                                | بيحصل فيها إيه                |
| ------------------------------------- | ----------------------------- |
| `/system/vehicles/registry/overview`  | كارت لكل عمود بعدّاده         |
| `/system/vehicles/registry`           | سجل المركبات                  |
| `/system/vehicles/registry/expiring`  | الانتهاءات والتجديد           |
| `/system/vehicles/registry/claims`    | الحوادث والمطالبات            |
| `/system/vehicles/registry/corporate` | وثائق تأمين الشركة            |
| `/system/vehicles/registry/alerts`    | التنبيهات                     |
| `/system/vehicles/registry/settings`  | **مدة التنبيه لكل نوع مستند** |
| `/system/vehicles/registry/dashboard` | التحليلات                     |

### الورك فلو ⑦: التفويض (من يقود أي عربية)

```
تعيين → نقل لموظف تاني → سحب
كل خطوة بتتسجّل بتاريخها، والتاريخ بيظهر في تبويب داخل ملف الموظف نفسه

حالة المستند محسوبة مش مخزّنة:
  valid / warning / critical / expired / missing / not_applicable
  و«غير مطلوب» عمرها ما تتحسب ناقصة

التجديد: تسجّل «اتجدد لحد إمتى» فالحالة بتتحسب من الأول
مدة التنبيه لكل نوع مستند بتتظبط من صفحة الإعدادات، مش ثابتة في الكود
```

---

## ٢.٦ الورشة · Workshop

`/api/workshop` — الأدوار: `workshop_manager` / `workshop_employee`

`/system/workshop/store` (المستودع) · `/system/workshop` · `/system/workshop/purchases`
· `/system/workshop/dashboard` · `/system/workshop/tasks` (أوامر شغل المركبات)

**مبدأ المخزن:** المستودع **سجل واحد لكل حاجة الشركة تملكها** — المركّب والموجود
في الرف مع بعض. المشتريات بتغذّيه.

**قاعدة أمان:** الحركة المسجّلة **ما بتتعدّلش وما بتتمسحش**. الغلط بيتصحّح
بحركة معاكسة مربوطة بالأصلية، والاتنين يفضلوا ظاهرين باسم اللي عملهم وتاريخهم.

> السبب: «طالما الموظف عمل حاجة مينفعش يعدّل — للأمان والسيكيوريتي».

---

## ٢.٧ المشتريات · Procurement

`/api/procurement` — `procurement_manager` / `procurement_staff`

`/dashboard` · `/requests` · `/orders` · `/bills`

### الورك فلو ⑧

```
طلب شراء:   draft → pending_approval → approved → ordered
                                    └→ rejected
أمر شراء:   draft → sent → partially_received → received → billed
                                                        └→ cancelled
فاتورة المورد → المحاسبة (الذمم الدائنة)
```

---

## ٢.٨ المحاسبة · Accounting

`/api/accounting` — `finance_manager` / `accountant`

`/dashboard` · `/accounts` (شجرة الحسابات) · `/journal` (القيود) ·
`/trial-balance` (ميزان المراجعة) · `/profit-loss` (الأرباح والخسائر) ·
`/receivables` (المدينون) · `/payables` (الدائنون)

---

## ٢.٩ إدارة العلاقات · CRM

`/api/crm` + `/api/crm-vendors` — `crm_manager` / `crm_team_lead` / `crm_specialist` / `crm_agent`

`/dashboard` · `/companies` · `/vendors` (موردي 3PL) · `/contacts` · `/deals` ·
`/tasks` · `/calendar` · `/activities` · `/customer-kpis` · `/vendor-kpis`

**قاعدة بيانات عملاء هجينة:** العملاء بيتجمّعوا من سجلات كل الأقسام في قايمة واحدة.
وأزرار تواصل مباشرة (اتصال / واتساب / إيميل) من على الكارت نفسه.

---

## ٢.١٠ المبيعات · Sales

`/api/sales` — `sales_manager` / `sales_rep`

`/dashboard` · `/pipeline` · `/targets` (المستهدفات) · `/performance`

---

## ٢.١١ التسويق · Marketing

`/api/marketing` — `marketing_manager` / `marketing_specialist`

`/system/marketing` · `/campaigns` · `/activities` · `/reports`

---

## ٢.١٢ تطوير الأعمال · Business Development

`/api/business-development` — `bd_manager` / `bd_specialist`

`/system/bd` · `/opportunities` · `/partners` · `/tenders` (المناقصات)

---

## ٢.١٣ تقنية المعلومات · Software & IT

`/api/it` — `it_manager` / `it_specialist`

`/system/it` · `/tickets` · `/recurring` (المشكلات المتكررة) · `/custody` (العهد
والأجهزة) · `/stock` · `/systems` (الأنظمة والخدمات) · `/emails` (بريد الشركة)

**بريد الشركة:** صناديق البريد منفصلة تمامًا عن حسابات الدخول على النظام. كلمات
السر متخزّنة بتشفير AES-256-GCM قابل للاسترجاع، **وكل عملية كشف بتتسجّل**.

**قاعدة الملكية:** العهد التقنية بتاعة IT، وعهد الموظفين بتاعة الموارد البشرية —
سجلّين مختلفين لغرضين مختلفين.

---

## ٢.١٤ العقود · Contracts

`/api/contracts` — `contracts_manager` / `contracts_staff`

`/system/contracts` · `/vendors` (سجل موردي 3PL) · `/analysis` (تحليل التشغيل
الفعلي) · `/prospects` (تنشيط الموردين الجدد) · `/agreements` (عقود الأقسام)

**ملاحظة تقنية:** الربط بين السجلات بيتم بمطابقة أسماء عربية مطبّعة (توحيد
أ/إ/آ، ة/ه، ى/ي، وشيل التشكيل) — لأن نفس الشركة بتتكتب بخمس طرق.

---

## ٢.١٥ منصة الأوبريشن · Operations Platform

`/api/ops` — `ops_platform_manager` / `ops_platform_staff`

مرآة حيّة لنظام عمليات ميداني خارجي. ١٧ صفحة: الشحنات، السائقون، المركبات،
أصحاب المركبات، العملاء، الفروع، المدن، الدول، أنواع/أحجام الشاحنات، أنواع
الحمولة، ماركات وألوان المركبات.

التكامل: `x-api-key` + JWT عبر بروكسي `/api/ops`، وتحديث بالـpolling ثم سوكت.

---

## ٢.١٦ طلبات الشحنات · Shipment Orders

`/api/shipment-orders` — `shipment_orders_manager` / `shipment_orders_staff`

`/system/shipment-orders` · `/chat` (مساعد إنشاء) · `/customers` ·
`/fleet` (الموردون والمركبات) · `/form-settings` (إعدادات النموذج)

**الفكرة:** إنشاء شحنة **محليًا** بدل النظام الخارجي. نموذج صفحة واحدة، وحقوله
**مبنية من إعدادات** مش ثابتة في الكود — فالمستخدم يزوّد حقل من غير مبرمج.
أسعار خطوط لكل عميل. البوليصة بتبدأ من رقم ٥٠٠.

---

## ٢.١٧ قطاع الأفراد · B2C

`/api/b2c` + `/api/b2c-wallet` — `b2c_manager` / `b2c_project_lead`

`/dashboard` · `/reps-performance` · `/daily-entry` · `/reps` · `/projects` ·
`/custody` (العهدة)

**العهدة:** دفتر أستاذ لمديري المشاريع (وارد / منصرف / رصيد). المديرون بيمنحوا
ويراجعوا. معزول تمامًا عن باقي B2C.

---

## ٢.١٨ العمل عن بُعد · Remote

`/api/remote` — `remote_manager` / `remote_employee`

`/attendance` (الحضور) · `/dashboard` · `/leave` · `/chat` · `/tasks` ·
`/report` (التقرير اليومي) · `/announcements`

قسم مستقل بحضوره وإجازاته الخاصة، مربوط بحسابات الدخول مش بسجلات الموظفين.

---

## ٢.١٩ الشؤون الإدارية (السكرتارية) · Administration

`/api/admin-tasks` — `administration_manager` / `administration_staff`

`/system/administration` — **شاشة واحدة** بلوحة على طريقة Trello.

---

## ٢.٢٠ مراجعة الأعمال · Business Review

`/api/business-review` — الأدوار: كل مديري الأقسام

`/system/business-review` (الاجتماعات) · `/my-actions` (البنود المسندة إليّ) ·
`/actions` (سجل المتابعة) · `/my-tasks`

### الورك فلو ⑨: اجتماع من الدعوة للإقفال

```
دورية الاجتماع: يومي / لحظي / أسبوعي / شهري / ربع سنوي

الحالات — واتنين منهم مختلفين خالص:
   مجدول  →  **انعقد**  (الاجتماع اتعمل)
                  ↓
             **اكتمل**  (كل البنود والمهام المتعلقة خلصت — بإعلان من الإدارة)

انعقد ≠ اكتمل. اجتماع ممكن ينعقد وبنوده تفضل مفتوحة شهور.
كروت قابلة للضغط: المكتمل × اللي لسه مفتوح.
```

### الرؤية — مفروضة بالتصميم مش بالفلترة

```
تنفيذي (مدير عام / admin / IT)  →  كل اجتماع، كل محضر، كل بند، كل تكليف
سكرتارية (administrator)        →  نفس ده، وهي اللي بتكتب المحاضر وترفع البنود
مدير قسم                        →  الاجتماعات اللي اتدعى لها + بنوده + تكليفاته
موظف                            →  **التكليفات الموجّهة له وبس**

السطر الأخير مفروض بالموديل نفسه: اندبوينت الموظف بيقرا من مجموعة (BrAssignment)
**ما فيهاش محتوى اجتماع أصلًا**. يعني مستحيل يتسرّب حتى لو الفلتر اتكسر.
```

---

## ٢.٢١ تقييم الأداء · Performance / KPI

`/api/performance`

`/system/performance` (تقييم مديري الأقسام) · `/overview` (نظرة كل الأقسام) ·
`/requests` (طلبات تعديل التقييم) · `/settings` (إعداد المؤشرات)

### الورك فلو ⑩

```
مؤشرات موزونة → درجة مرجّحة (×٢٠) → فئة → مكافأة في المرتّبات

من يقيّم من:
  super_admin / admin / IT  →  الشركة كلها، وهم وحدهم يعدّلوا النماذج
                               والأوزان والفئات ومستويات المكافأة
  مدير قسم                  →  اللي بيرفعوا له (Employee.directManager) + قسمه

الدرجة بتتحسب في **مكان واحد** (computeScore) عشان المعاينة الحيّة والسجل
المحفوظ والـPDF ما يختلفوش أبدًا.
```

---

## ٢.٢٢ العملاء والمالية · Customers & Finance

`/api/invoices` `/api/payments` `/api/collections` `/api/disputes`

`/system/invoices` · `/payments` · `/collections` · `/collectors` · `/tasks` ·
`/disputes` · `/system/overdue` · `/system/credit-alerts`

---

# ٣. الأقسام الخدمية — Cross-cutting

## ٣.١ الإدارة · Admin

`/system/users` (المستخدمون) · `/system/permissions` (**الأدوار والصلاحيات**) ·
`/system/branches` · `/system/expense-categories` ·
`/system/settings/reference-data` (القوائم المرجعية) · `/system/audit` (سجل
التدقيق) · `/system/complaints`

## ٣.٢ الأدوات · Tools

`/system/reports` (مركز التقارير) · `/system/kpis` · `/system/assistant` ·
`/system/settings`

## ٣.٣ النظرة التنفيذية · Executive Overview

`/system/executive` — لـsuper_admin بس. بتنادي **داشبورد كل قسم بنفسه** وتجمّع
النتايج في كروت مجمّعة قابلة للضغط + رسوم بيانية، حيّة.

> الفكرة اللي تتنقل: ما تكتبش استعلامات جديدة للشاشة التنفيذية. نادِ نفس
> الاندبوينتات اللي الأقسام بتستعملها — كده الرقم فوق دايمًا = الرقم جوه.

## ٣.٤ بوابة الشركاء · Partner Portal

`/api/partners` (الإدارة) + `/api/portal` (البوابة نفسها)

`/system/portal` · `/shipments` (شحناتي) · `/customs` (معاملات التخليص) ·
`/invoices` · `/payments`

```
١) قايمة واحدة بتجمع **كل عميل ومورد** من سجلات كل الأقسام، وجنب كل صف اسم
   السجل اللي جه منه: «شركة كذا — عميل تخليص جمركي»

٢) أي صف فيهم يتحوّل لحساب دخول (إيميل + باسورد)، ويتدار بعدين
   (إعادة تعيين، تعطيل، فك الربط)

٣) البوابة **ديناميكية**: الحساب مربوط بسجل واحد، لكن العميل نادرًا بيعيش في
   سجل واحد — عميل النقل الثقيل عنده فواتير كمان، وعميل التخليص بيحجز عربيات.
   فالبوابة بتلمّ كل حاجة تخصّه من كل الأقسام حسب هو مين بالظبط.
```

---

# ٤. الترابط بين الأقسام — Cross-section flows

## الحاجات اللي بتعدّي من قسم لقسم

```
① موظف جديد
   الموارد البشرية (سجل الموظف)
      → IT (حساب دخول + عهدة جهاز + بريد شركة)
      → المركبات (تفويض قيادة لو سائق)
      → تقييم الأداء (بيدخل تحت مديره)
      → الخدمة الذاتية (يقدر يطلب إجازة ويشوف ملفه)

② كاوتش من الشراء للسكراب
   المشتريات (أمر شراء)
      → مخزن الورشة / مخزن النقل الثقيل (وارد)
      → سجل الأصول (تسجيل الفردة بسيريالها)
      → تركيب على عربية (mounted، والقديمة لازم تروح مكان محدد)
      → تحت التجديد → مجدد (تيدر بس) أو سكراب
      → بيع كخردة
   وكل خطوة بتظهر في **تاريخ العربية** جنب قطع الغيار والصيانة

③ عربية عندها حادث
   المركبات (مطالبة + وثيقة التأمين)
      → الموارد البشرية (السائق — بيظهر في ملفه)
      → الورشة (أمر شغل إصلاح)
      → المحاسبة (تكلفة الإصلاح)
      → لوكيشن سوليوشن (سجل الأصول لو اتغيّر كاوتش)

④ شحنة لعميل
   إدارة العلاقات (العميل موجود في السجل)
      → إدارة الأسطول (حجز عربية + سائق) أو طلبات الشحنات (عربية مورد)
      → التخليص الجمركي (لو استيراد)
      → البوليصة PDF
      → العملاء والمالية (فاتورة)
      → التحصيل → المحاسبة (قيد)
      → بوابة الشركاء (العميل بيشوف شحنته وفاتورته بنفسه)

⑤ قرار من اجتماع
   مراجعة الأعمال (بند على مدير قسم)
      → المدير يكلّف موظف (تكليف)
      → الموظف يشوف التكليف **بس** في «البنود المسندة إليّ»
      → الإقفال بيرجع لسجل المتابعة
      → «اكتمل» لما كل حاجة تخلص

⑥ أي شغل في أي قسم
   «مهامي» + «الشكاوى» في كل قسم (نفس المكوّن)
      → إشعار لحظي للمُسنَد إليه (ويب + موبايل بصوت)
      → مرئي للمُسنَد إليه والمُنشئ وsuper_admin **وبس**
```

---

# ٥. القواعد اللي اتدفع تمنها — Hard-won rules

## دي الحاجات اللي لو نقلتها لمشروع تاني هتوفّر عليك نفس الأخطاء

### داتا

1. **اربط بالمعرّف الثابت مش بالمتسلسل.** الرقم الوظيفي بيتعاد استخدامه، ورقم
   اللوحة بيتغيّر. استعمل رقم الهوية / الشاسيه / السيريال.
2. **«ناقص» تصريح مش سكوت.** فرّق بين «الحقل ده مطلوب وناقص» و«لا ينطبق» و«الشيت
   مش شايله». الفرق ده هو اللي بيحوّل داشبورد لقايمة شغل.
3. **ارفض التناقض، ما تبلعوش.** نفس السيريال على عربيتين = وقف واطبع مين، مش آخر
   صف يكسب.
4. **نسخة احتياطية قبل أي مسح**، واحتفظ بالـ`_id` عشان مراجع الأقسام التانية
   ما تتكسرش.
5. **العلَم يتشتق من مصدره في الموديل** (`pre('save')`) مش في كل مسار — وإلا
   هيتوه من الباب اللي نسيته.

### واجهة

6. **الليبل ما يكدبش.** «عدد الموظفين» ما يتحركش مع فلتر؛ لو الرقم مفلتر اكتبه.
7. **الرقم مدخل مش زينة.** أي عدّاد على الشاشة لازم يفتح الصفوف اللي وراه.
8. **الأزرار تعريف واحد.** لو نفس الأكشن في مكانين، اعمله مكوّن — النسخ معناه
   إنهم هيختلفوا بعد أول تعديل.
9. **سطر واحد للصف**، والنص واضح مش رمادي فاتح، والجدول يـscroll أفقيًا لوحده.

### خلفية

10. **افرض القاعدة في السيرفر** عشان تسري على الويب والموبايل وأي مسار تاني —
    والواجهة تقول القاعدة **قبل** ما السيرفر يرفض.
11. **اقفل كل الأبواب مش باب واحد.** قاعدة «الموقع ما يفضلش فاضي» لازم تتحط على
    الإنزال **و**الإتلاف **و**نقل الحالة.
12. **السجل append-only.** الغلط يتصحّح بحركة معاكسة، مش بتعديل السطر.
13. **الحساب في مكان واحد** عشان المعاينة والمحفوظ والـPDF ما يختلفوش.

### تشغيل

14. **الديبلوي مش خلص لما الملفات توصل، خلص لما الحاجة تشتغل.** افحص: المسارات
    بترد 401 مش 404، السوكت 101، هيدر الـCORS، الـ.env والمرفقات عاشوا، ومسار
    وهمي بيرد 404 (وإلا الفحوصات فوق مالهاش معنى).
15. **اختم الكوميت المنشور** عشان سؤال «البرودكشن شغّال على أنهي نسخة؟» يبقى ليه
    إجابة.
16. **التيست اللي بيكتب، يكتب على داتا بتاعته** ويمسحها، وفي الآخر يتأكد إنه ما
    ساب أثر.
17. **افحص على الداتا الحقيقية.** الفحص المبني على افتراضات بيعدّي وهو غلط.

---

# ٦. إزاي تطلب قسم زيهم — Prompt template

> انسخ الجزء ده وحطّه لكلود مع القسم اللي عايزه:

```
اعملّي قسم «<اسم القسم>» في نظام ERP عربي/إنجليزي (Next.js App Router +
Express/MongoDB + Socket.IO)، بنفس الآلية دي:

الصلاحيات:
  • دورين: <section>_manager و <section>_staff
  • القسم يتسجّل في config/sections.js بـ key و apiPrefixes
  • sectionGate يتركّب على المسارات: none تقفل كله، view تقفل الكتابة
  • القسم يفتح **بالكامل بكل أكشناته** لأي دور اتفتح له

الصفحات المتكرّرة في كل قسم:
  • لوحة القسم (كروت أرقام، كل رقم قابل للضغط ويفتح صفوفه)
  • الصفحات الأساسية للقسم: <عدّدها>
  • «مهامي» و«الشكاوى» من /api/section-work بنفس قاعدة الرؤية الصارمة
  • «تقييم الأداء»

الجداول:
  • عمود لكل حقل + المعرّف اللي بيتسيرش بيه له عموده
  • سطر واحد للصف، نص واضح، ترتيب على كل عمود، فلاتر شغّالة فعلًا
  • تصدير Excel للمعروض
  • responsive: الجدول يـscroll لوحده والصفحة ما تتحركش أفقيًا

الحيّ:
  • حدث سوكت باسم القسم، وكل الشاشات تشترك فيه
  • كاش TTL قصير بيتمسح عند أي كتابة
  • إشعار لحظي للمُسنَد إليه (ويب + موبايل)

اللغة:
  • عربي/إنجليزي كامل + RTL من نفس مصدر الترجمة

التوثيق والتيست:
  • سكربت تدقيق بيشتغل على الـAPI الحقيقي وبيطبع «نجح كذا · فشل كذا»
  • مستخدمو الاختبار بـ zz- وبيتمسحوا، والتيست ما يسيبش أثر

والموبايل (Flutter) يتعمل معاه في نفس الشغلة بنفس السلوك.
```

---

_مقروء من الكود بتاريخ ٢٠٢٦-٠٨-٠٥ · Generated from the live codebase_
